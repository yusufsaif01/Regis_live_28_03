{
    "host": {
        "doc": "server host",
        "format": "String",
        "default": "0.0.0.0",
        "env": "SERVER_HOST",
        "arg": "SERVER_HOST"
    },
    "port": {
        "doc": "server port",
        "format": "Number",
        "default": 2001,
        "env": "SERVER_PORT",
        "arg": "SERVER_PORT"
    }
}